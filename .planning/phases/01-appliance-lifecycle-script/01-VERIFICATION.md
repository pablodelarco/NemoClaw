---
phase: 01-appliance-lifecycle-script
verified: 2026-03-24T15:37:42Z
status: passed
score: 12/12 must-haves verified
---

# Phase 1: Appliance Lifecycle Script Verification Report

**Phase Goal:** The appliance runtime logic is complete -- appliance.sh handles installation, configuration, and bootstrap of NemoClaw with full contextualization support and graceful GPU fallback
**Verified:** 2026-03-24T15:37:42Z
**Status:** PASSED
**Re-verification:** No -- initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | appliance.sh implements service_install(), service_configure(), and service_bootstrap() following the one-apps lifecycle convention | VERIFIED | All four lifecycle functions present and fully implemented at lines 53, 299, 420, 592. File is 605 lines, passes `bash -n`. |
| 2 | ONE_SERVICE_PARAMS defines all contextualization parameters (API key, model, sandbox name) with correct types, defaults, and descriptions | VERIFIED | Lines 42-46: ONEAPP_NEMOCLAW_API_KEY as `M|password`, ONEAPP_NEMOCLAW_MODEL as `O|text`, ONEAPP_NEMOCLAW_SANDBOX_NAME as `O|text`. ONE_SERVICE_RECONFIGURABLE=true at line 33. |
| 3 | service_configure() reads CONTEXT variables and writes NemoClaw configuration (API key to restricted file, model selection, sandbox name) | VERIFIED | Lines 309-358: reads all three CONTEXT vars with defaults, calls store_api_key() which writes to /etc/nemoclaw/api_key with chmod 0600 (line 283), writes nemoclaw.conf with 0600 permissions (line 358). |
| 4 | service_bootstrap() creates and starts the NemoClaw sandbox, then runs health validation | VERIFIED | Lines 420-590: sources nemoclaw.conf, calls `nemoclaw onboard`, `nemoclaw create`, `nemoclaw start`, then check_nemoclaw_health() with 30-retry loop. On success writes READY=YES report and success MOTD. |
| 5 | GPU detection logic differentiates GPU-present (proceed with nvidia-smi validation) from GPU-absent (log warning, continue with remote-only inference) | VERIFIED | detect_gpu() lines 183-226: GPU-present path validates with nvidia-smi, captures GPU_MODEL/GPU_MEMORY, regenerates CDI spec. GPU-absent path at lines 222-224 sets GPU_DETECTED=false and logs "NemoClaw will use remote NVIDIA Endpoints inference only." |

**Score:** 5/5 success criteria verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `appliances/nemoclaw/appliance.sh` | Complete appliance lifecycle script with all three functions implemented | VERIFIED | Exists, executable (-rwxr-xr-x), 605 lines, passes `bash -n` syntax check. All four lifecycle functions fully implemented (no stubs). |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| appliance.sh service_install | NemoClaw installer | `curl -fsSL https://www.nvidia.com/nemoclaw.sh` | WIRED | Line 21: NEMOCLAW_INSTALL_URL constant; line 136-139: downloaded, sed-patched to skip run_onboard, executed. |
| appliance.sh service_install | Docker official repo | `apt-get install docker-ce` | WIRED | Lines 67-79: Docker GPG key, repo added, docker-ce + docker-ce-cli + containerd.io + plugins installed. |
| appliance.sh service_configure | CONTEXT variables | ONEAPP_NEMOCLAW_API_KEY env var | WIRED | Lines 305-320: reads ONEAPP_NEMOCLAW_API_KEY (mandatory), ONEAPP_NEMOCLAW_MODEL with default, ONEAPP_NEMOCLAW_SANDBOX_NAME with default. |
| appliance.sh detect_gpu | NVIDIA GPU hardware | lspci and nvidia-smi | WIRED | Lines 188 and 197: `lspci 2>/dev/null | grep -qi 'nvidia'` then `nvidia-smi` for validation. |
| appliance.sh service_configure | credentials file | write API key to restricted file | WIRED | Lines 282-284: `echo "${_api_key}" > "${NEMOCLAW_API_KEY_FILE}"` then `chmod 0600`. |
| appliance.sh service_bootstrap | NemoClaw CLI | nemoclaw onboard and nemoclaw create | WIRED | Lines 472-531: `nemoclaw onboard --api-key ... --model ... --non-interactive` with env-var fallback; `nemoclaw create`; `nemoclaw start`. |
| appliance.sh service_bootstrap | config from service_configure | sources nemoclaw.conf | WIRED | Lines 425-426: `. "${ONE_SERVICE_SETUP_DIR}/nemoclaw.conf"` -- sources config generated by service_configure. |

---

### Data-Flow Trace (Level 4)

Not applicable. appliance.sh is a shell lifecycle script -- it has no dynamic data rendering (no UI components, no API responses to render). Its "data" is configuration values read from CONTEXT env vars and written to config files, which is verified at Level 3 (wiring).

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Bash syntax validity | `bash -n appliances/nemoclaw/appliance.sh` | Exit 0, "SYNTAX OK" | PASS |
| File is executable | `ls -la appliances/nemoclaw/appliance.sh` | `-rwxr-xr-x` | PASS |
| Line count >= 250 (plan requirement) | `wc -l` | 605 lines | PASS |
| No remaining stubs (colon-only bodies) | grep for stub patterns | No TODO/FIXME/PLACEHOLDER found | PASS |
| All lifecycle functions present | grep count | 4 functions defined: service_install, service_configure, service_bootstrap, service_cleanup | PASS |
| ONE_SERVICE_RECONFIGURABLE=true set | grep | Line 33: `ONE_SERVICE_RECONFIGURABLE=true` | PASS |
| API key stored with 0600 permissions | grep | Line 283: `chmod 0600 "${NEMOCLAW_API_KEY_FILE}"` | PASS |
| NVIDIA_API_KEY unset after bootstrap use | grep | Lines 501 and 507: `unset NVIDIA_API_KEY` in both success and failure paths | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| LIFE-01 | 01-01-PLAN.md | appliance.sh implements service_install() for package installation during Packer build | SATISFIED | service_install() at line 53 installs all 12 dependency steps: prerequisites, Docker CE, log rotation, Docker autostart, NVIDIA driver 550-server, NVIDIA Container Toolkit + CDI config, Node.js 22, NemoClaw (non-interactive), sandbox image pre-pull, systemd-networkd-wait-online disable, swap, postinstall_cleanup. |
| LIFE-02 | 01-02-PLAN.md | appliance.sh implements service_configure() for first-boot configuration from CONTEXT variables | SATISFIED | service_configure() at line 299: reads all three CONTEXT vars with defaults, stores API key securely, detects GPU, ensures Docker running, writes nemoclaw.conf, writes MOTD. |
| LIFE-03 | 01-03-PLAN.md | appliance.sh implements service_bootstrap() for NemoClaw sandbox creation and startup | SATISFIED | service_bootstrap() at line 420: sources nemoclaw.conf, validates API key file, calls `nemoclaw onboard`, `nemoclaw create`, `nemoclaw start`, runs check_nemoclaw_health() with 30-retry loop. |
| LIFE-04 | 01-01-PLAN.md | ONE_SERVICE_PARAMS array defines all contextualization parameters with types and descriptions | SATISFIED | Lines 42-46: three-entry array with correct format ('VAR_NAME' 'stage' 'description' 'M/O|type'). API key as M|password, model and sandbox name as O|text. |
| LIFE-05 | 01-01-PLAN.md | ONE_SERVICE_RECONFIGURABLE=true enables recontext without redeploy | SATISFIED | Line 33: `ONE_SERVICE_RECONFIGURABLE=true`. service_cleanup() at line 592 removes nemoclaw.conf and API key file to enable clean recontext. |
| CTX-01 | 01-01-PLAN.md | ONEAPP_NEMOCLAW_API_KEY (mandatory password) injects NVIDIA API key for inference | SATISFIED | Line 43: defined as `M|password`. Lines 305-335: read in service_configure, passed to store_api_key(), stored to /etc/nemoclaw/api_key. |
| CTX-02 | 01-01-PLAN.md | ONEAPP_NEMOCLAW_MODEL (optional list) selects Nemotron model, default nemotron-3-super-120b | SATISFIED | Line 44: defined as `O|text`. Line 309: `export ONEAPP_NEMOCLAW_MODEL="${ONEAPP_NEMOCLAW_MODEL:-nemotron-3-super-120b}"`. |
| CTX-03 | 01-01-PLAN.md | ONEAPP_NEMOCLAW_SANDBOX_NAME (optional text) sets sandbox instance name, default "nemoclaw" | SATISFIED | Line 45: defined as `O|text`. Line 312: `export ONEAPP_NEMOCLAW_SANDBOX_NAME="${ONEAPP_NEMOCLAW_SANDBOX_NAME:-nemoclaw}"`. |
| CTX-04 | 01-02-PLAN.md | SSH key-based access via $USER[SSH_PUBLIC_KEY] contextualization | SATISFIED | Lines 365-368: explicitly documented in code comments that SSH key access is handled by OpenNebula context agent via $USER[SSH_PUBLIC_KEY]. No custom code needed -- correct per one-apps convention. `network-manager` installed in service_install() (line 61) per PITFALLS.md. |
| CTX-05 | 01-02-PLAN.md | Network auto-configuration via standard OpenNebula contextualization | SATISFIED | Lines 368-370: explicitly documented that network auto-configuration is handled by addon-context-linux package and NetworkManager. Correct architectural decision per OpenNebula convention. |
| GPU-02 | 01-02-PLAN.md | Appliance detects GPU presence at boot and adjusts behavior accordingly | SATISFIED | detect_gpu() at line 183: three-level cascade (lspci -> /dev/nvidia* device nodes -> nvidia-smi validation). Sets GPU_DETECTED=true/false; result written to nemoclaw.conf (line 354) and used in service_bootstrap. |
| GPU-04 | 01-02-PLAN.md | When GPU is absent, appliance logs warning and continues with remote-only inference mode | SATISFIED | Lines 222-224: `GPU_DETECTED=false`, logs "No functional NVIDIA GPU detected. NemoClaw will use remote NVIDIA Endpoints inference only." service_bootstrap continues regardless of GPU state (uses NEMOCLAW_GPU_DETECTED from conf but does not abort when false). |

**All 12 requirements: SATISFIED**

---

### Anti-Patterns Found

No blockers or warnings. The following patterns were scanned and cleared:

| Pattern | Result |
|---------|--------|
| TODO/FIXME/PLACEHOLDER comments | None found |
| Stub-only function bodies (bare colon) | None -- all four lifecycle functions fully implemented |
| Empty returns (return null / return {}) | Not applicable (bash script) |
| Hardcoded empty data passed to rendering | Not applicable (bash script, no rendering) |

---

### Human Verification Required

#### 1. msg Helper Function Contract

**Test:** Source the appliance.sh in a context that provides `msg` (from the one-apps framework) and verify the logging calls work correctly.
**Expected:** `msg info`, `msg warning`, `msg error` calls write to the appliance log without erroring.
**Why human:** The `msg` function is provided by the one-apps framework at runtime, not by appliance.sh itself. The script depends on it being present in the execution environment. Programmatic verification would require the full one-apps framework to be present on the test host.

#### 2. postinstall_cleanup Function Contract

**Test:** Verify `postinstall_cleanup` is available in the one-apps execution environment when service_install() runs.
**Expected:** Called at line 173, clears apt caches and temp files as per the one-apps convention.
**Why human:** Same reason -- provided by one-apps framework, not definable without the full build environment.

#### 3. NemoClaw --non-interactive Flag Compatibility

**Test:** On a system with NemoClaw installed, run `nemoclaw onboard --non-interactive` and verify it accepts the flag.
**Expected:** Either succeeds with `--non-interactive`, or falls through to the env-var fallback path at lines 481-503.
**Why human:** NemoClaw is alpha software and the `--non-interactive` flag may not exist in all versions. The script has a fallback, but verifying which path executes requires a live NemoClaw installation.

#### 4. GPU Passthrough Behavior

**Test:** Deploy the appliance on an OpenNebula host with a physical NVIDIA GPU configured for PCI passthrough, and verify detect_gpu() sets GPU_DETECTED=true and the CDI spec is generated.
**Expected:** GPU_DETECTED=true, nvidia-smi outputs GPU info, /etc/cdi/nvidia.yaml is written.
**Why human:** Requires physical GPU hardware and OpenNebula VM with PCI passthrough -- cannot verify in a static code analysis.

---

### Gaps Summary

No gaps. All 12 requirements (LIFE-01 through LIFE-05, CTX-01 through CTX-05, GPU-02, GPU-04) are satisfied by the implementation in `appliances/nemoclaw/appliance.sh`. The file is 605 lines, passes bash syntax check, is executable, and contains fully implemented (non-stub) bodies for all four lifecycle functions.

The four human verification items are operational concerns that cannot be verified statically -- they do not block phase completion.

---

_Verified: 2026-03-24T15:37:42Z_
_Verifier: Claude (gsd-verifier)_
