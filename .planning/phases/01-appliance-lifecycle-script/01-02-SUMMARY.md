---
phase: 01-appliance-lifecycle-script
plan: 02
subsystem: infra
tags: [bash, gpu-detection, nvidia, opennebula, contextualization, api-key, motd]

# Dependency graph
requires:
  - phase: 01-appliance-lifecycle-script
    plan: 01
    provides: appliance.sh skeleton with service_configure() stub, ONE_SERVICE_PARAMS, constants
provides:
  - service_configure() with CONTEXT variable handling and config file generation
  - detect_gpu() with lspci/nvidia-smi validation and CDI spec regeneration
  - store_api_key() with 0600 restricted file storage and env var cleanup
  - write_motd() with dynamic GPU/sandbox/model status
  - nemoclaw.conf generated for service_bootstrap consumption
affects: [01-03, packer-build, testing]

# Tech tracking
tech-stack:
  added: []
  patterns: [GPU detection via lspci + nvidia-smi + device nodes, secure credential storage with 0600 permissions, CONTEXT variable defaults, config file generation for cross-phase state]

key-files:
  created: []
  modified:
    - appliances/nemoclaw/appliance.sh

key-decisions:
  - "GPU detection runs in service_configure (not service_bootstrap) to set GPU_DETECTED flag before config file generation"
  - "API key stored to /etc/nemoclaw/api_key with 0600 permissions and env var unset to minimize exposure"
  - "nemoclaw.conf written to ONE_SERVICE_SETUP_DIR as bridge between configure and bootstrap phases"
  - "CTX-04 (SSH) and CTX-05 (network) documented as handled by one-context, not coded"

patterns-established:
  - "Secure credential pattern: read from CONTEXT, write to restricted file, unset env var"
  - "GPU detection cascade: lspci -> device nodes -> nvidia-smi -> CDI spec regen"
  - "Error MOTD pattern: write clear error message with remediation steps to /etc/motd on failure"
  - "Cross-phase config: nemoclaw.conf generated by configure, consumed by bootstrap"

requirements-completed: [LIFE-02, CTX-04, CTX-05, GPU-02, GPU-04]

# Metrics
duration: 3min
completed: 2026-03-24
---

# Phase 1 Plan 02: Service Configure and GPU Detection Summary

**service_configure() with CONTEXT variable handling (API key, model, sandbox name defaults), GPU detection via lspci/nvidia-smi cascade, secure API key storage with 0600 permissions, and nemoclaw.conf generation for bootstrap**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-24T15:23:33Z
- **Completed:** 2026-03-24T15:26:34Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Implemented detect_gpu() with full GPU detection cascade: lspci check, device node verification, nvidia-smi validation, GPU model/memory capture, and CDI spec regeneration via nvidia-ctk
- Implemented store_api_key() with secure credential handling: 0600 permissions, root ownership, environment variable cleanup after storage, and clear error messaging when key is missing
- Implemented write_motd() with dynamic MOTD showing GPU status, sandbox name, model, NemoClaw version, and alpha software warning
- Replaced service_configure() stub with full implementation: CONTEXT variable defaults, API key storage, GPU detection, Docker readiness check, config file generation, and MOTD writing

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement GPU detection helper functions** - `c1f5d84` (feat)
2. **Task 2: Implement service_configure() with CONTEXT variable handling** - `8ce042c` (feat)

## Files Created/Modified
- `appliances/nemoclaw/appliance.sh` - Added detect_gpu(), store_api_key(), write_motd() helpers and replaced service_configure() stub with full implementation

## Decisions Made
- GPU detection runs in service_configure rather than service_bootstrap, setting the GPU_DETECTED flag early so the config file captures GPU state for bootstrap consumption
- API key is stored to `/etc/nemoclaw/api_key` (not `~/.nemoclaw/credentials.json`) with 0600 permissions, and ONEAPP_NEMOCLAW_API_KEY is unset from the environment after storage to minimize exposure surface
- nemoclaw.conf is written to ONE_SERVICE_SETUP_DIR as a bridge between configure and bootstrap phases, containing all resolved configuration values
- CTX-04 (SSH key access) and CTX-05 (network configuration) are documented as handled by the standard OpenNebula one-context agent, requiring no custom code

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- service_configure() is complete and ready for service_bootstrap() implementation in Plan 03
- nemoclaw.conf provides all configuration values that bootstrap needs
- GPU_DETECTED flag is available for bootstrap to decide on GPU-specific setup
- service_bootstrap() and service_cleanup() remain as stubs ready for Plan 03

## Self-Check: PASSED

- FOUND: appliances/nemoclaw/appliance.sh
- FOUND: commit c1f5d84
- FOUND: commit 8ce042c
- FOUND: 01-02-SUMMARY.md

---
*Phase: 01-appliance-lifecycle-script*
*Completed: 2026-03-24*
