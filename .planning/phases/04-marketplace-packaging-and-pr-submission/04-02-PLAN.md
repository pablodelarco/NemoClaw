---
phase: 04-marketplace-packaging-and-pr-submission
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - appliances/nemoclaw/UUID.yaml
autonomous: false
requirements:
  - MKT-01
  - MKT-11

must_haves:
  truths:
    - "UUID-named YAML metadata file contains all required marketplace fields"
    - "YAML has correct checksums (md5, sha256) from the built image"
    - "YAML has correct virtual size from qemu-img info"
    - "YAML references a publicly accessible image URL"
    - "YAML user_inputs section is empty (simplified appliance, no context params)"
    - "PR is submitted and /marketplace-check validation passes"
  artifacts:
    - path: "appliances/nemoclaw/UUID.yaml"
      provides: "Marketplace metadata with image URL, checksums, template"
      contains: "NemoClaw"
  key_links:
    - from: "appliances/nemoclaw/UUID.yaml"
      to: "logos/nemoclaw.png"
      via: "logo field references logo path"
      pattern: "logos/nemoclaw.png"
    - from: "appliances/nemoclaw/UUID.yaml"
      to: "image hosted URL"
      via: "images[0].url points to downloadable qcow2"
      pattern: "url:"

user_setup:
  - service: build-host
    why: "Checksums and image size must be extracted from the built image on the build host"
    env_vars: []
    dashboard_config:
      - task: "SSH to build host to get checksums"
        location: "ssh root@100.123.42.13"
  - service: image-hosting
    why: "The qcow2 image must be hosted at a publicly accessible URL for the marketplace"
    env_vars: []
    dashboard_config:
      - task: "Upload nemoclaw qcow2 to CDN or web server and get URL"
        location: "User must decide hosting location"
---

<objective>
Create the UUID-named marketplace YAML metadata file with real checksums from the built image, and submit the PR to OpenNebula/marketplace-community.

Purpose: The UUID YAML is the core marketplace entry that makes the appliance discoverable and downloadable. It requires real data from the built image (checksums, size) and a hosted image URL. The PR submission triggers the /marketplace-check validation bot.

Output: UUID.yaml file with all required fields, PR submitted to marketplace-community.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/04-marketplace-packaging-and-pr-submission/04-01-SUMMARY.md
@appliances/nemoclaw/appliance.sh
@appliances/nemoclaw/metadata.yaml

<interfaces>
<!-- From Prowler PR #99 UUID YAML, the exact structure needed: -->

Required top-level fields:
- name, version, one-apps_version, publisher, publisher_email
- description (multiline), short_description
- tags (list), format, creation_time (epoch seconds)
- os-id, os-release, os-arch, hypervisor, opennebula_version
- opennebula_template (with context, cpu, vcpu, graphics, memory, name)
- logo (path to logo file)
- images (list with name, url, type, dev_prefix, driver, size, checksum)

Key NemoClaw-specific values:
- Publisher: Pablo del Arco (pdelarco@opennebula.io)
- Base OS: Ubuntu 24.04 (os-release: '24.04')
- Image virtual size: 42949672960 bytes (40 GiB) -- confirm with qemu-img info
- Format: qcow2
- No user_inputs (simplified appliance has no context params)
- opennebula_template needs PCI section for GPU passthrough (vendor 10de)
- logo: logos/nemoclaw.png

Build host commands for checksums:
```bash
ssh root@100.123.42.13 'md5sum /root/marketplace-community/apps-code/community-apps/output/nemoclaw'
ssh root@100.123.42.13 'sha256sum /root/marketplace-community/apps-code/community-apps/output/nemoclaw'
ssh root@100.123.42.13 'qemu-img info --output=json /root/marketplace-community/apps-code/community-apps/output/nemoclaw | jq ".\"virtual-size\""'
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Gather image checksums and hosting URL</name>
  <what-built>The built qcow2 image exists on the build host at /root/marketplace-community/apps-code/community-apps/output/nemoclaw</what-built>
  <how-to-verify>
Three pieces of information are needed from the build host and from the user:

**Step 1: Get checksums from build host**
Run these commands (or have Claude run them via SSH):
```bash
ssh root@100.123.42.13 'md5sum /root/marketplace-community/apps-code/community-apps/output/nemoclaw'
ssh root@100.123.42.13 'sha256sum /root/marketplace-community/apps-code/community-apps/output/nemoclaw'
ssh root@100.123.42.13 'qemu-img info --output=json /root/marketplace-community/apps-code/community-apps/output/nemoclaw | jq ".\"virtual-size\""'
```

**Step 2: Upload image and provide URL**
The qcow2 image needs to be uploaded to a publicly accessible location (CDN, S3 bucket, web server).
Provide the URL where the image will be downloadable (e.g., https://your-cdn.com/nemoclaw-0.1.0-1.qcow2).

**Step 3: Provide results**
Reply with:
- MD5 checksum
- SHA256 checksum
- Virtual size in bytes
- Public image URL
  </how-to-verify>
  <resume-signal>Provide: md5=..., sha256=..., size=..., url=...</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create UUID YAML and submit PR</name>
  <files>
    appliances/nemoclaw/UUID.yaml
  </files>
  <read_first>
    appliances/nemoclaw/appliance.sh
    appliances/nemoclaw/metadata.yaml
    appliances/nemoclaw/README.md
  </read_first>
  <action>
**1. Generate UUID for filename:**
```bash
UUID=$(uuidgen | tr '[:upper:]' '[:lower:]')
```

**2. Create appliances/nemoclaw/${UUID}.yaml** (MKT-01):

Use the exact Prowler PR #99 structure. Fill in with the values from Task 1 checkpoint:

```yaml
---
name: NemoClaw AI Agent Security
version: 0.1.0-1
one-apps_version: 7.0.0-0
publisher: Pablo del Arco
publisher_email: pdelarco@opennebula.io
description: |-
  NemoClaw is NVIDIA's open-source AI agent security stack built on OpenClaw.
  It provides sandboxed execution environments for AI agents with deny-by-default
  network policies, inference routing through NVIDIA Endpoints, and real-time
  monitoring via OpenShell TUI.

  **Key Features:**
  - GPU passthrough support for local inference (optional)
  - Docker-based OpenShell sandbox with Landlock/seccomp isolation
  - NVIDIA inference routing to Nemotron models via build.nvidia.com
  - Deny-by-default network policies for AI agent containment
  - OpenShell TUI for real-time agent activity monitoring
  - GPU detection with graceful fallback to cloud-only inference

  **This appliance provides:**
  - Ubuntu 24.04 LTS base operating system
  - Docker Engine with NVIDIA Container Toolkit
  - NVIDIA driver 550-server pre-installed
  - Node.js 22 LTS pre-installed
  - NemoClaw CLI pre-installed and ready for onboarding
  - 4 GB swap space configured
  - SSH and VNC access

  **Getting Started:**
  1. Deploy from OpenNebula marketplace
  2. SSH into the VM (root / opennebula)
  3. Run: nemoclaw onboard
  4. Follow the interactive setup wizard

  **Documentation:**
  - [NemoClaw Documentation](https://docs.nvidia.com/nemoclaw/latest/)
  - [NemoClaw GitHub](https://github.com/NVIDIA/NemoClaw)
  - [Appliance Guide](https://github.com/OpenNebula/marketplace-community/blob/master/appliances/nemoclaw/README.md)

  **WARNING:** NemoClaw is alpha software. APIs and behavior may change between releases.

short_description: NVIDIA AI agent security stack with GPU passthrough and sandboxed execution
tags:
- nemoclaw
- nvidia
- ai-agent
- security
- gpu
- docker
- openclaw
- openshell
- x86_64
format: qcow2
creation_time: EPOCH_TIMESTAMP
os-id: Ubuntu
os-release: '24.04'
os-arch: x86_64
hypervisor: KVM
opennebula_version: '6.8..7.0'
opennebula_template:
  context:
    network: 'YES'
    ssh_public_key: $USER[SSH_PUBLIC_KEY]
    set_hostname: $USER[SET_HOSTNAME]
  cpu: '2'
  vcpu: '2'
  graphics:
    listen: 0.0.0.0
    type: vnc
  memory: '8192'
  os:
    arch: x86_64
  cpu_model:
    model: host-passthrough
  name: NemoClaw AI Agent Security
logo: logos/nemoclaw.png

images:
- name: nemoclaw-0.1.0-1.qcow2
  url: >-
    IMAGE_URL_FROM_CHECKPOINT
  type: OS
  dev_prefix: vd
  driver: qcow2
  size: VIRTUAL_SIZE_FROM_CHECKPOINT
  checksum:
    md5: MD5_FROM_CHECKPOINT
    sha256: SHA256_FROM_CHECKPOINT
```

Replace placeholders:
- `EPOCH_TIMESTAMP`: Use `$(date +%s)` at creation time
- `IMAGE_URL_FROM_CHECKPOINT`: The URL provided in Task 1
- `VIRTUAL_SIZE_FROM_CHECKPOINT`: The virtual size in bytes from Task 1
- `MD5_FROM_CHECKPOINT`: The MD5 checksum from Task 1
- `SHA256_FROM_CHECKPOINT`: The SHA256 checksum from Task 1

Important notes:
- `opennebula_version: '6.8..7.0'` uses range so appliance appears for multiple OpenNebula versions
- `cpu_model.model: host-passthrough` is required for GPU passthrough
- No `user_inputs` or `inputs_order` fields (simplified appliance has no context params)
- `size` field MUST be the virtual size from `qemu-img info`, NOT the file size
- The `os` section only needs `arch: x86_64` (firmware/machine set by admin based on GPU needs)

**3. Submit PR to marketplace-community** (MKT-11):

This step depends on the repository structure. If Pablo's NemoClaw repo IS a fork of marketplace-community or contains the files in the right structure, create a PR. Otherwise, document the files that need to be copied to the marketplace-community fork for PR submission.

If submitting:
```bash
# Create branch
git checkout -b feature/nemoclaw-appliance

# Stage all marketplace files
git add appliances/nemoclaw/ logos/nemoclaw.png apps-code/community-apps/

# Commit
git commit -m "feat: add NemoClaw AI Agent Security appliance"

# Push and create PR
git push origin feature/nemoclaw-appliance
gh pr create --repo OpenNebula/marketplace-community \
  --title "Add NemoClaw AI Agent Security appliance" \
  --body "..."
```

If the repo structure does not support direct PR, create a summary document listing all files that need to be included in the PR to OpenNebula/marketplace-community.
  </action>
  <verify>
    <automated>ls appliances/nemoclaw/*.yaml | grep -v metadata.yaml | grep -v context.yaml | head -1 | xargs -I{} sh -c 'grep -q "NemoClaw" {} && grep -q "qcow2" {} && grep -q "logos/nemoclaw.png" {} && grep -q "checksum" {} && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - UUID-named YAML file exists in appliances/nemoclaw/
    - YAML contains all required fields: name, version, publisher, description, tags, format, os-id, os-release, os-arch, hypervisor, opennebula_version, images
    - images section has real checksums (not "pending"), real URL, real virtual size
    - opennebula_template has host-passthrough CPU model for GPU support
    - logo field points to logos/nemoclaw.png
    - creation_time is set to current epoch
    - No user_inputs (simplified appliance)
  </acceptance_criteria>
  <done>UUID YAML exists with all required fields, real checksums, and image URL. PR submitted or PR-ready file set documented.</done>
</task>

</tasks>

<verification>
Complete PR file set validation:
```bash
# List all files that should be in the PR
echo "=== PR File Checklist ==="
for f in \
  appliances/nemoclaw/appliance.sh \
  appliances/nemoclaw/metadata.yaml \
  appliances/nemoclaw/README.md \
  appliances/nemoclaw/CHANGELOG.md \
  appliances/nemoclaw/context.yaml \
  appliances/nemoclaw/tests.yaml \
  appliances/nemoclaw/tests/00-nemoclaw_basic.rb \
  logos/nemoclaw.png \
  apps-code/community-apps/Makefile.config \
  apps-code/community-apps/packer/nemoclaw/nemoclaw.pkr.hcl \
  apps-code/community-apps/packer/nemoclaw/variables.pkr.hcl \
  apps-code/community-apps/packer/nemoclaw/gen_context \
  apps-code/community-apps/packer/nemoclaw/cloud-init.yml \
  apps-code/community-apps/packer/nemoclaw/81-configure-ssh.sh \
  apps-code/community-apps/packer/nemoclaw/82-configure-context.sh; do
  if [ -f "$f" ]; then echo "  [OK] $f"; else echo "  [MISSING] $f"; fi
done
# Check UUID YAML separately
ls appliances/nemoclaw/*.yaml | grep -v metadata.yaml | grep -v context.yaml | while read f; do echo "  [OK] $f (UUID YAML)"; done
```
</verification>

<success_criteria>
- UUID YAML has real checksums, real virtual size, real image URL
- All marketplace PR files exist (16 files total across appliances/, apps-code/, logos/)
- PR submitted to OpenNebula/marketplace-community or PR-ready documentation provided
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace-packaging-and-pr-submission/04-02-SUMMARY.md`
</output>
